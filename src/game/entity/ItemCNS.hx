package game.entity;

import types.EItem;

class ItemCNS {
	public var type( default, default ) : EItem;
	public var level( default, default ) : Int = 0;
	public var value( default, default ) : Float = 0;

	public function new() {
	}
}